/** Shared max width for homepage hero, sections, nav, and footer — wider on desktop without full-bleed copy on ultrawide. */
export const LANDING_CONTENT_MAX =
  "max-w-[min(1320px,calc(100%-2rem))]";
